List all custom Claude commands available in this project by reading the files in `.claude/commands/`.

For each command, show:
- the command name (filename without .md, prefixed with /)
- one sentence describing what it does

Format as a numbered list.
