Here are all available custom commands for this project:

1. `/project-status` — summarize current project phase, completed work, and next steps from PROJECT_STATUS.md
2. `/continue-project` — read project state and ask what to work on next
3. `/next-task` — recommend the single most valuable next implementation step
4. `/list-commands` — show this list of available custom commands
